<h1>New car</h1>

<%= form_for @car, url: { action: :create } do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.label :category %>
	<%= f.select :category, ["A", "B", "C"] %>

	<%= f.submit %>
<% end %>